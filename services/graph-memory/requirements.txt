# =============================================================================
# MCP Memory Service - Dépendances Python
# =============================================================================
# Direct dependency input. The image installs the generated, hash-locked
# `requirements.lock`, not this file directly. Regenerate intentionally with:
# uv pip compile requirements.txt --universal --generate-hashes --no-annotate \
#   --no-header --output-file requirements.lock
# Review the complete resolution and build-test amd64 + arm64 on every bump.
# =============================================================================

# === MCP SDK (Streamable HTTP transport) ===
mcp==1.28.1

# === Cryptography runtime (PyJWT/MCP transitif) ===
# CVE-2026-69247 affecte les versions 44.x a 49.x.
cryptography==50.0.0

# === Web Framework (pour FastMCP Streamable HTTP) ===
fastapi==0.139.2
uvicorn[standard]==0.51.0
starlette==1.3.1

# === HTTP Client (async) ===
httpx==0.28.1

# === Neo4j Driver ===
neo4j==6.2.0

# === Qdrant Vector DB ===
qdrant-client==1.18.0

# === S3 Client (AWS SDK) ===
boto3==1.43.82

# === OpenAI-compatible client (pour LLMaaS) ===
openai==2.46.0

# === Configuration ===
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# === Utilitaires ===
tenacity==9.1.4           # Retry logic avec backoff
python-multipart==0.0.32  # File uploads
aiofiles==25.1.0          # Async file operations
PyYAML==6.0.3             # Parsing des ontologies YAML

# === Document Processing ===
python-docx==1.2.0        # Lecture fichiers .docx
pypdf==6.14.2             # Lecture fichiers PDF

# === HTML Processing ===
beautifulsoup4==4.15.0

# === CLI ===
click==8.4.2               # Framework CLI
rich==15.0.0               # Affichage CLI (tableaux, spinners, couleurs)
aiohttp==3.14.3            # CVE-2026-69244 (parser C de reponses malformees)

# === Tests (optionnel, pour dev) ===
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# pytest-cov>=4.0.0
